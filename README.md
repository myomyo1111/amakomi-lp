# amakomi-lp
Public 
cd /workspaces/amakomi-lp && \
git pull && \
cp "amakomi_lp_final (1).html" index.html && \
git add index.html && \
git rm "amakomi_lp_final (1).html" && \
git commit -m "index.html に修正" && \
git push
